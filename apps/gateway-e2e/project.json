{
  "name": "gateway-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["gateway", "expense"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config apps/gateway-e2e/vitest.config.ts"
      }
    }
  }
}
