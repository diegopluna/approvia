{
  "name": "notifications",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/notifications/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["generate-prisma"],
      "options": {
        "command": "webpack-cli build",
        "args": ["--node-env=production"],
        "cwd": "apps/notifications"
      },
      "configurations": {
        "development": {
          "args": ["--node-env=development"]
        }
      }
    },
    "generate-prisma": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{workspaceRoot}/apps/notifications/src/generated/prisma"],
      "options": {
        "command": "pnpm notifications:db:generate"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["generate-prisma"],
      "options": {
        "command": "vitest run --config apps/notifications/vitest.config.ts"
      }
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "notifications:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "notifications:build:development"
        },
        "production": {
          "buildTarget": "notifications:build:production"
        }
      }
    }
  }
}
